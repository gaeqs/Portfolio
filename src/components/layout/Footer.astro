---
import Social from "../ui/Social.astro";
import {Icon} from "astro-icon/components";
import {AstroError} from "astro/errors";
import {getCollection} from "astro:content";

const [staticData] = await getCollection('staticData');

if (!staticData) {
  throw new AstroError("JSON data not found");
}
---

<footer
        class="relative bottom-0 w-full px-4 py-8 font-medium text-blacktext dark:bg-transparent dark:border-b-2 dark:border-zinc-800 dark:text-zinc-300 max-lg:mt-3"
        role="contentinfo"
        aria-label="Site footer"
>
  <nav
          class="mx-auto flex max-w-7xl flex-row items-center justify-between gap-4 text-xl max-xl:px-6 max-sm:flex-col text-xs font-mono text-zinc-600"
          aria-label="Footer navigation"
  >
    <div class="flex items-center gap-2">
      <span class="w-2 h-2 rounded-full bg-green-500/20 border border-green-500/50"></span>
      <span>© {new Date().getFullYear()} Gael Rial. All systems nominal.</span>
    </div>
    <span>Built with Astro & Tailwind</span>
  </nav>
</footer>
